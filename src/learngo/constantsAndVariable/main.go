package main

import "fmt"

func main() {
	// Constants
	const constName string = "onestep"

	var varName string = "onestep"
	name := "Auto allocate variable type -> string"
	fmt.Println(varName)
	fmt.Println(name)
}
